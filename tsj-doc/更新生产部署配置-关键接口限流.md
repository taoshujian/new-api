# 更新生产部署配置（关键接口限流）

- **日期**：2026-07-08
- **提交**：`daa42eb1` chore: 更新部署配置
- **类型**：部署配置变更

## 变更内容

仅修改 `docker-compose.prod.yml`（+4 / -1）：

1. 注释修正：`连接宿主机 MySQL / Redis` → `连接容器 MySQL / Redis`。
2. 新增关键接口限流环境变量：
   - `CRITICAL_RATE_LIMIT_ENABLE=true`：启用关键接口限流
   - `CRITICAL_RATE_LIMIT=200`：限流阈值
   - `CRITICAL_RATE_LIMIT_DURATION=1200`：限流窗口时长（秒）
